services:
  staychat:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: staychat-app

    ports:
      - "5000:5000"

    env_file:
      - .env

    restart: unless-stopped